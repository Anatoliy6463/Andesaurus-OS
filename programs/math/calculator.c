#include <stdio.h>
int main(void)
{
int A;
int B;
int C;
char znak;
scanf (%d, %A);
scanf (%d, %B);
scanf (%c, %znak);
if (znak == '+') C = A + B;
if (znak == '-') C = A + B;
if (znak == '*') C = A + B;
if (znak == '/') C = A + B;
if (znak == '^') 
{
int a2;
for (int i = 0; i < A; i++)
{
a2*=a;
}
printf (a2);
return (0);
}
printf (C);
return (0);
}
